export function SettingsScreen() {
  return (
    <article className="screen-panel" aria-labelledby="settings-title">
      <p className="panel-kicker">Configuration</p>
      <h2 id="settings-title">Settings</h2>
      <p>
        Settings shell. T008 will add persisted settings and control
        preferences.
      </p>
    </article>
  );
}
