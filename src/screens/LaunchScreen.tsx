export function LaunchScreen() {
  return (
    <article className="screen-panel" aria-labelledby="launch-title">
      <p className="panel-kicker">Phase 1</p>
      <h2 id="launch-title">Launch</h2>
      <p>
        Launch phase shell. T004 will add simulation logic; T005 will add Phaser
        rendering.
      </p>
    </article>
  );
}
